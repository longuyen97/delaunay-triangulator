dist: bionic
sudo: required
language:
  - cpp
  - python
python:
  - "3.7"
addons:
  apt:
    packages:
      - gcc-7
      - g++-7
      - cmake
before_script:
  - pip install conan
script:
  - mkdir build && cd build
  - conan install .. && cmake ..
  - make
  - ./bin/tests